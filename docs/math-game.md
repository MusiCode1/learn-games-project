להלן **Specification (מפרט)** מסודר וברור כדי לבנות את המשחק.

---

# 1. מטרת המשחק

ללמד חיבור כ”המשך רצף” (counting on):
התלמיד בונה רכבת של X קרונות, ואז **מוסיף עוד Y** קרונות בקצה, כך שהוא לומד ש־`X + Y` הוא “המספר הבא אחרי X” שוב ושוב — בלי לספור מחדש מהתחלה.

המשחק חייב:

- לכפות תהליך (בניית קרונות) לפני בחירת תשובה
- להגביל ניחוש (מעט אפשרויות + קירור בין לחיצות + מגבלת טעויות)
- להדגיש חזותית את שתי הקבוצות בצבעים שונים על אותה שורה רציפה

---

# 2. קהל יעד והנחות UX

- אוטיזם תפקוד נמוך: עומס חזותי נמוך, חזרתיות, שפה קצרה, משוב מיידי.
- אינטראקציה: גרירה/הקשה פשוטה בלבד.
- אין טקסט ארוך; כל הוראה גם בקול וגם באיקון.

---

# 3. זרימת משחק (Flow)

## 3.1 תצורת תרגיל (Round)

כל סיבוב בוחר:

- `A` (מספר קרונות ראשונים) בטווח למשל 1–5
- `B` (מספר קרונות להוסיף) בטווח 1–4
- `correct = A + B`
- `choices` = מערך של 3 תשובות: `[correct, distractor1, distractor2]` (ראו סעיף 7)

## 3.2 שלבי סיבוב

1. **Build A**: “שים A קרונות”
2. **Add B**: “הוסף עוד B קרונות”
3. **Choose**: “כמה קרונות יש עכשיו?” + 3 אפשרויות
4. **Feedback**: נכון/לא נכון
5. מעבר לסיבוב הבא / חזרה לשלב בחירה (בטעויות)

---

# 4. מסכים וממשקים (UI Spec)

## 4.1 מסך ראשי (GameScreen)

רכיבים:

### HeaderBar

- כפתור בית/איפוס
- אינדיקציה קצרה: סיבוב X / הצלחות (אופציונלי)

### InstructionPanel (למעלה או בצד)

- טקסט קצר מאוד (לדוגמה: “שים 3 קרונות”)
- תצוגה חזותית של המספר (למשל: 3 נקודות/אייקונים)
- כפתור “השמע שוב” (🔊)

### TrainTrackArea (האזור המרכזי)

- מסילה אופקית
- קרונות מוצגים בשורה אחת
- **קרונות A בצבע קבוצה 1**, קרונות B בצבע קבוצה 2
- **רווח קטן** (GapMarker) בין קבוצה 1 לקבוצה 2 (רק אחרי שלב ההוספה)

התנהגות:

- הקרון האחרון “מואר” לרגע כאשר מוסיפים קרון חדש (הדגשת המשכיות)
- אנימציה קצרה של הצמדה לקצה הימני (snap)

### DepotArea (מאגר קרונות)

- כפתור/אזור “הוסף קרון” גדול (עדיף Tap ולא Drag למי שמתקשה)
- אופציה: גם גרירה של קרון בודד למסילה
- בשלב Build A: הקרונות שמתווספים הם “סוג A”
- בשלב Add B: הקרונות שמתווספים הם “סוג B”

### CounterBadge (עוגן לרצף)

- אחרי סיום Build A מופיע Badge קטן ליד הרכבת: “כבר יש A”
- בזמן Add B אפשר להציג: “ממשיכים…” + מספר קרונות שהוספו מתוך B (למשל 1/2)

### AnswerPanel (מופיע רק בשלב Choose)

- שאלה: “כמה קרונות יש עכשיו?”
- 3 כפתורי תשובה גדולים מאוד עם מספרים (ללא 1–10)
- נעילת לחיצות בין ניסיונות (cooldown)

---

# 5. אינטראקציות וחוקים

## 5.1 בניית קרונות

- המשתמש מוסיף קרון אחד בכל פעולה (Tap על כפתור “+ קרון” או גרירה)
- המערכת סופרת פנימית `builtCount`
- בשלב Build A: נעילה אוטומטית אחרי `builtCount == A` (לא מאפשר עוד)
- בשלב Add B: מתחילים מ־0 תוספות ומאפשרים עד `B`

## 5.2 מניעת “ספירה מחדש”

אסור לתת מצב שבו לחיצה על הקרונות מקריאה “1,2,3,…” או מציגה מיספור מלא.
אם רוצים תמיכה: להדגיש קבוצות (A צבע1, B צבע2) + רווח + “היו A, הוספת B”.

## 5.3 בחירת תשובה

- `AnswerPanel` פעיל רק אחרי השלמת A וגם B
- 3 אפשרויות בלבד
- מגבלת טעויות לסיבוב: למשל 2
- cooldown בין לחיצות: למשל 1500ms
- אחרי X טעויות: הצגה של פתרון מודרך (ראו 6.3)

---

# 6. משוב (Feedback)

## 6.1 תשובה נכונה

- צליל קצר “✓”
- אנימציה: כל הרכבת “קופצת” קלות / דגלון קטן
- מעבר אוטומטי לסיבוב הבא אחרי 800–1200ms

## 6.2 תשובה שגויה (ניסיון 1)

- צליל קצר ניטרלי “×” (לא דרמטי)
- המערכת לא משנה את הרכבת
- הדגשה מחדש של המבנה:

  - הקבוצה הראשונה מהבהבת בצבע1
  - הקבוצה השנייה מהבהבת בצבע2
  - טקסט/קול: “היו A. הוספת B. נסה שוב.”

- חוזרים לבחירה, עם cooldown

## 6.3 תשובה שגויה (אחרי 2 טעויות באותו סיבוב) — “הדרכה”

- מוצג מסךון overlay קצר:

  - `A` אייקונים בצבע1 + `B` אייקונים בצבע2 → “זה יחד …”

- ואז:

  - מציגים שוב את אותן 3 אפשרויות (או מצמצמים ל-2 אפשרויות: correct ועוד אחת)

- לא לחשוף “נכון הוא 5” בצורה טקסטואלית ארוכה; להראות חזותית.

---

# 7. יצירת מסיחים (Distractors)

כללים ליצירת 2 מסיחים:

- להיות קרובים לתשובה כדי שלא יהפוך לניחוש קל מדי, אבל לא “טריק” קוגניטיבי
- להימנע ממסיחים רחוקים (כמו 10 כשנכון 4)

אלגוריתם מומלץ:

- `d1 = correct - 1` אם >= 0
- `d2 = correct + 1`
- אם אחד מתנגש בטווח/כפילויות, להשתמש ב:

  - `A` (הטעות הנפוצה: לא מוסיף בכלל)
  - `B` (הטעות הנפוצה: מתבלבל בין הקבוצות)
    סוף: לבחור 3 ייחודיים, לערבב סדר.

דוגמה:

- A=3, B=2, correct=5 → choices: [4,5,6]
- A=4, B=1, correct=5 → choices: [4,5,6] (עדיף)
- A=1, B=4, correct=5 → choices: [1,5,6] או [4,5,6] לפי יכולת

---

# 8. קולות וטקסטים (Content Spec)

כל משפט קצר, עקבי, אותו ניסוח תמיד.

סט משפטים:

- “שים {A} קרונות.”
- “הוסף עוד {B} קרונות.”
- “כמה קרונות יש עכשיו?”
- “נכון.”
- “לא. היו {A}. הוספת {B}. נסה שוב.”
- “בוא נראה יחד.”

אפשרות להחליף מילים לפי תלמיד: “קרונות”/“קוביות”/“חיות”.

---

# 9. פרמטרים להתאמה (Teacher Settings)

מסך הגדרות (אופציונלי, או JSON config):

- `maxA` (ברירת מחדל 5)
- `maxB` (ברירת מחדל 4)
- `choicesCount` (2 או 3)
- `cooldownMs` (1500)
- `maxWrongAttemptsPerRound` (2)
- `assistModeAfterWrong` (on/off)
- `inputMode` = “tap” / “drag” / “both”
- `visualGapBetweenGroups` (on/off)
- `voice` (on/off)

---

# 10. מצב (State Machine) מומלץ

States:

- `INIT`
- `BUILD_A`
- `ADD_B`
- `CHOOSE_ANSWER`
- `FEEDBACK_CORRECT`
- `FEEDBACK_WRONG`
- `ASSIST_OVERLAY`
- `NEXT_ROUND`

State data:

- `A, B, correct`
- `builtA` (0..A)
- `addedB` (0..B)
- `wrongAttempts`
- `choices[]`
- `cooldownUntilTs`

---

# 11. דרישות נגישות/חזות (Visual)

- כפתורים גדולים (מינימום ~80px גובה במובייל)
- ניגודיות גבוהה
- בלי רקעים עמוסים
- אנימציות קצרות (200–400ms), לא מסיחות
- צבעי הקבוצות קבועים לאורך כל המשחק

---

# 12. קריטריוני הצלחה (Acceptance Criteria)

1. אי אפשר להגיע למסך בחירה בלי לבנות A ולהוסיף B.
2. מוצגות לכל היותר 3 אפשרויות תשובה.
3. אי אפשר ללחוץ תשובות בקצב מהיר (cooldown עובד).
4. במקרה טעות, המשחק מדגיש “היו A” ו”הוספת B” חזותית.
5. הקרונות תמיד בשורה אחת רציפה, עם סימון גבול בין הקבוצות.
6. אין פונקציה שמאפשרת ספירה מחדש קלה (כמו הקראת 1..N על הקרונות).
